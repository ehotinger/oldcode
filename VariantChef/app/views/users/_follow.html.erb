<%= form_for(current_user.relationships.build(followed_id: @user.id),
             remote: true) do |f| %>
  <%= f.hidden_field :followed_id %>

  <%= button_tag "Follow", class: "btn btn-xs btn-primary" %>
<% end %>